# mission-control:managed version=2.0.0
interface:
  display_name: "Mission Control"
  short_description: "Manage project tasks and report agent progress"
  default_prompt: "Use $mission-control to inspect the current project, manage its canonical tasks, and report meaningful work boundaries."
